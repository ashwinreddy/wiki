**Hamming(7,4)** is a [[Hamming code]] for 4 bits of data with 3 bits. Let's say the data bits are $abcd$ and the parity bits are $xyz$

\begin{gather}
x = a \xor b \xor d \\\\
y = a \xor c \xor d \\\\
z = b \xor b \xor d \\\\
\end{gather}