**Q-Learning** train the [[Q-function|action-value function]] by iteratively applying the Bellman (optimality?) operator. Then, a greedy policy can be extracted by computing $\pi(s) = \arg\max_a Q(s,a)$.