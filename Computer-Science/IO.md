**IO** is input/output. Low-level I/O is buffered in [[kernel]] space. Think `open`, `creat`, `close`.

High-level I/O uses a user space buffer and streams. In [[C]], think `fopen`, `fread`, `fwrite`, etc.

