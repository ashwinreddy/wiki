The **Erlang distribution** is a sum of [[exponential distribution]].

$$
X_i \sim \mathsf{Exp}(\lambda) \implies \sum_{i=1}^k X_k \sim \mathsf{Erlang}(k, \lambda)
$$

$$
f(x; k, \lambda) = \frac{\lambda\^k x\^{k-1}e^{-\lambda x}}{(k-1)!}
$$