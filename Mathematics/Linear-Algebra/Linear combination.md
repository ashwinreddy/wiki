A **linear combination** is constructed by the weighted sum of [[vectors|vector]]. For a set of vectors $\\{v_1, \dots, v_n\\}$, a linear combination is any expression of the form

$$
\sum_{i=1}^n \alpha_i v_i
$$