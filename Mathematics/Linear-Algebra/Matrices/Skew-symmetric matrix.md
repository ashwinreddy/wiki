A **skew-symmetric matrix** is a [[square matrix]] whose negative is equal to its [[transpose]].

\begin{equation}
A^\mathsf{T} \equiv -A
\end{equation}

# Properties

* The [[matrix exponential]] of a skew-symmetric matrix is an [[orthogonal matrix]].