The **tensor product** of [[vector spaces|vector space]] is a composite vector space that allows for admixture unlike [[direct sum]]. The easiest example to grok is the outer product

$$
v \otimes w = vw^\mathsf{T}
$$