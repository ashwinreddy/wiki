An **injection** is a [[function]] that is left-unique. Heuristically, it means the function is "invertible" on its image. Formally,

\begin{equation}
x \neq y \implies f(x) \neq f(y)
\end{equation}

