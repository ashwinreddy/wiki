The **electric field** is a [[vector field]] that assigns a [[vector]] to each point in space by imagining the [[force]] on a small test charge $\vec{q}$

$$
\vec{E}(\vec{r}) = \frac{\vec{F}(\vec{r})}{q}
$$