The **Fisher information** is a measurement for how much information a [[random variable]] $X$ carries about the parameter $\theta$ that generates it. Formally, it is computed using the [[score]].

$$
\mathcal{I}_X(\theta) \triangleq \mathbb{E}_{p_\theta}[s(\theta)^2]
$$