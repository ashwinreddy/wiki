**Maximum likelihood estimation (MLE)** estimates the parameters of a [[statistical model]] by picking the value that maximizes the [[likelihood function]] $L(\theta \mid x)$.

$$
\hat{L}(\theta; y) = p_\theta(y) \tag{Likelihood function}
$$

$$
\hat\theta_{\mathsf{MLE}} = \arg\max_{\theta \in \Theta} \hat{L}(\theta ; y)
$$
