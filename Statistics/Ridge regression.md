**Ridge regression** is an extension of [[ordinary least squares]] where

$$
\hat\beta = (A^\top A + \lambda I)^{-1} A^\top \vec{y}
$$

$$
L = \lVert A\vec{x} - \vec{b} \rVert^2_2 + \lambda \lVert \vec{w} \rVert^2_2
$$