**Ridge regression** is an extension of least squares [[linear regression]] where we add a multiple $\lambda$ of the [[identity matrix]] before inversion. [[Design matrix]] $X$

\begin{equation}
\beta = (X\^\mathsf{T}X + \lambda I)\^{-1}X^\mathsf{T}y
\end{equation}